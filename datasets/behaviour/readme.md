Here are the **behavior datasets** used for supervised fine-tuning:

The directory 'en' contains the datasets of the **English version**.

The directory 'zh' contains the datasets of the **Chinese version**.

